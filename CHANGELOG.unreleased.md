<!--
  Черновик заметок к следующему релизу.
  Дописывайте сюда по одной строке-пункту сразу после каждого заметного изменения
  в приложении (пока свежо в памяти, а не в момент релиза).

  При запуске scripts/release.ps1 без -Notes содержимое этого файла (без этого
  комментария) станет текстом релиза на GitHub и текстом в changelog.html.
  После успешной публикации файл автоматически очищается и коммитится пустым,
  готовый к следующему циклу.
-->

- Импорт стинтов из Google Таблиц теперь забирает и время старта первого стинта
  в «Старт гонки», поэтому импортированный план показывает те же часы, что и
  таблица, а не уезжает на разницу со старым «Стартом гонки».
- В редакторе стинтов добавлены колонка «Конец» (правится — меняет длительность),
  время суток под «Стартом» и «Концом», кнопка «Пересчитать по-своему…» (с какого
  стинта, с какого смещения, общая длительность и пит) и флажок «Автопересчёт
  стартов после правок».
